# Active specs

Design specs for the **current** phase only. **Empty = no open spec** — the last phase
(cockpit redesign, ADR-055) shipped 2026-06-12.

Completed specs are in `../archive/specs/` (retrieval only — do not execute them).

Next phase: brainstorm it (`superpowers:brainstorming`), then drop the validated spec
here before writing a plan.
