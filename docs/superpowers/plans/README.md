# Active plans

Implementation plans for the **current** phase only. **Empty = no open plan** — the last
phase (cockpit redesign, ADR-055) shipped 2026-06-12.

Completed plans are in `../archive/plans/` (retrieval only — do not execute them).

Next phase: write the plan here with `superpowers:writing-plans` once its spec is validated.
