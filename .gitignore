# Private local docs — never push
LOCAL_PROJECT_STORY.md
.kiro/

# Personal financial data — never commit
data/personal/

# AI assistant scaffolding & internal process docs — keep local, never push
.claude/
CLAUDE.md
CONTEXT.md
AGENTS.md
MEMORY.md
docs/superpowers/
docs/fix-targets/
docs/external-review/
docs/design-references/
research/
docs/STATUS.md
docs/PHASE_LOG.md
docs/SKILL_ROUTING.md
sentiment_audit_results.csv

# Coverage artifacts
.coverage

# Auto-generated timestamped backtest dumps (curated reports are tracked individually)
data/reports/backtest_report_*.json
# Auto-generated weekly screen output (curated screen_ic_* exhibits are tracked individually)
data/reports/screen_20*.json
# Personal holdings review log — contains book-level output, never commit
data/reports/discipline_weekly_review.log
# Always-regenerating live reports — untracked to avoid pre-commit stash conflicts
data/reports/divergence_ic_21d.json
data/reports/momentum_discipline.json
data/reports/corroboration_weekly_resolve.log

# SP5 forward-gate resolution logs — local data, never commit
data/corroboration_samples.jsonl
data/corroboration_gate_log.jsonl

# Raw data — never commit large files to Git
data/raw/
data/interim/
data/processed/
data/cache/

# SQLite database (contains recommendation history)
*.db

# Hypothesis

# Pytest
.pytest_cache/
.hypothesis/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Environments
.env
.venv
env/

# MLflow
mlruns/
mlartifacts/
mlflow.db

# macOS
.DS_Store

# Conda env — requirements.txt covers deps
environment.yml

# Model artifacts
models/
*.pkl
*.joblib

.superpowers/
