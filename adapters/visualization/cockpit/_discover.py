"""Section 4 — look into next / discover."""

from __future__ import annotations

import streamlit as st


def render(
    *,
    summary_path: str,
    reports_dir: str,
    holdings_path: str,
) -> None:
    st.caption("Section pending build.")
