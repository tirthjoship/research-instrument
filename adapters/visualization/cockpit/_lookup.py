"""Section 5 — lookup / stock-detail."""

from __future__ import annotations

import streamlit as st


def render(*, reports_dir: str, summary_path: str) -> None:
    st.caption("Section pending build.")
