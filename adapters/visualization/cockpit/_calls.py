"""Section 2 — your calls."""

from __future__ import annotations

import streamlit as st


def render(
    *,
    summary_path: str,
    holdings_path: str,
    discipline_log_path: str,
    history_dir: str,
) -> None:
    st.caption("Section pending build.")
